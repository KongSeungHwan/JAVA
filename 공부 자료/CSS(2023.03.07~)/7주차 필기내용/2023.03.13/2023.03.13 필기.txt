복습
HTTP >> HyperTextTransferProtocol

html
hypertext markup language

<h1>~<h6>

<p>

<pre>

<b>

<strong>

<i>

<em>

<mark>

<ins>

<u>

<del>


.
.
.

선택자

전체선택자

id선택자

class 선택자

자손 선택자
일반 자식 선택자

인접 자식 선택자

속성 선택자


box model

content

padding

border

margin

outline


layout position

static

absolute

relative

fixed  viewport 기준 고정 자리

sticky(원래 자리에 있다 스크롤 내리믄 고정자리)


----------------------------------

UI

resize

outline-offset

box-sizing


다중 colum(요소 차지 공간을 분할하여 여러 칼럼을 만든다.)
column-count: 3

column-rule style,color width,gap

column-count : 몇개의 컬럼으로 나뉠지 정한다.
column-gap : 컬럼끼리의 간격을 정한다.
column-rule-style : 컬럼의 경계선의 형식을 정한다.
column-rule width : 컬럼의 경계선의 가로길이를 정한다
column-rule-color : 컬럼의 경계선의 색을 정한다.
column-rule : 위 column-rule 3개를 한번에 짧게 쓸수 있다.
column-span : 컬럼위에 헤더와 같은 element가 몇 컬럼의 길이만큼 설정할지 정한다.



flexible box

flex item

display: flex로 변경
flex
flex-grow
flex-basis

direction

row(열)
row-reverse
colum(행)
colum-reverse


flex-wrap(줄넘김 처리)

nowrap
수평나열

wrap
수평으로 나열하되 줄바꿈 처리

wrap-reverse
bottom에서 top으로 수평 나열하되 줄바꿈 처리

flex-flow(direction,flex-wrap 속성을 한번에 정의 가능한 속성)

justify-content(수평 나열)
flex-start,end
center
space-between(요소 간 공간 전부),around(요소간 공간을 앞뒤로 2분의1로 분할 처리)
,evenly(요소간 간격으로서가 아닌 격자로 처리)


align-items(수직 나열)
strech(default)
flex-start
flex-end
center
baseline

align-content
flex-wrap: wrap;//nowrap 줄바꿈 여부 
shrink
basis
grow

align-self

z-index
우선순위를 줘서 겹칠 경우 z축으로 쌓아서 나열

grid


세로로 나열하되
중간 중간에 60px margin 공백을 주며 너비가 600px 까지만 제대로 나오나
 그 미만으로 넘어간다면 제대로 안나오고 찌그러짐
