고차 함수

sort
map
filter
.
.
.
대충 generate함수가 fill인것만 알면 stream연산과 쓰는 방법은 똑같음
단지 자바 스크립트에서는 따로 스트림 객체를 생성할 필요가 없음
또 화살표 차이 자바 ->      자바 스크립트  =>


대충 자바때 스트림 연산 암기한 것에서 출발하여 상기시키면 그만!

toFixed() 소수점아래 몇째 자리까지 표기할 메소드

인자 값이 n일 때 소수점아래 n번째 자리까지 표기



자바 스크립트는 자바처럼 객체지향 아닌 함수형 이기에 따로 클래스는 존재하지 않으므로 
이 클래스의 역할을 대체하는 것이 프로토타입이다.

기본적으로 프로토타입은 object 자료형이다.(자바와 혼동x)


만든 객체와 객체의 프로토타입 관계를 프로토타입 체인이라고 부름
